server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/foodie_auth
    username: postgres
    password: Bruce@04
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # Configuration Email (Gmail SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: brucefoyet@gmail.com
    password: mdcnorffqywsefpo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

  # ✅ LIQUIBASE DOIT ÊTRE ICI (sous spring:, même niveau que datasource, jpa, mail)
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

app:
  name: FoodieDelivery
  email:
    from: brucefoyet@gmail.com
  frontend:
    url: http://localhost:4200

cors:
  allowed-origins: http://localhost:4200